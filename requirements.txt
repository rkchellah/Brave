# Brave v3.0 — Python dependencies
# Install into the project venv:
#   & ".\.venv\Scripts\python.exe" -m pip install -r requirements.txt

MetaTrader5==5.0.5640      # Broker connection, market data, order execution
firebase-admin>=6.5.0      # Realtime Database sync with the mobile app
langgraph==1.2.9           # DETECT → ANALYSE → RISK_CHECK → EXECUTE/HITL pipeline
openai==2.46.0             # DeepSeek client (OpenAI-compatible API)
finnhub-python==2.4.27     # News headlines
requests==2.32.5           # ForexFactory economic calendar
numpy==2.4.3               # Strategy indicators and pair scoring
